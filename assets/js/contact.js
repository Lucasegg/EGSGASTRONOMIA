(()=>{'use strict';const form=document.querySelector('#contato-form');if(!form)return;const destination='cervejaedubieer@gmail.com';const started=Date.now();const clean=(v,max)=>String(v||'').replace(/[<>\u0000-\u001F]/g,' ').replace(/\s+/g,' ').trim().slice(0,max);const button=form.querySelector('button[type="submit"]');if(button)button.textContent='Enviar por e-mail';const note=form.parentElement?.querySelector('.form-note');if(note)note.textContent='Ao enviar, seu aplicativo de e-mail será aberto com a mensagem pronta para envio.';form.addEventListener('submit',e=>{e.preventDefault();e.stopImmediatePropagation();if(document.querySelector('#website')?.value||Date.now()-started<1200)return;const nome=clean(document.querySelector('#nome')?.value,80);const telefone=clean(document.querySelector('#telefone')?.value,30);const assunto=clean(document.querySelector('#assunto')?.value,80);const mensagem=clean(document.querySelector('#mensagem')?.value,700);if(!nome||!mensagem){alert('Preencha seu nome e a mensagem.');return;}const subject=`EGS Gastronomia - Fale Conosco - ${assunto}`;const body=[`Nome: ${nome}`,telefone?`Telefone: ${telefone}`:'',`Assunto: ${assunto}`,'',mensagem].filter(Boolean).join('\n');window.location.href=`mailto:${destination}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;},true);})();